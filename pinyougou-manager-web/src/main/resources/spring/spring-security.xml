<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
						http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd">

    <!-- 不进行权限控制资源 -->
    <http pattern="/*.html" security="none"/>
    <http pattern="/css/**" security="none"/>
    <http pattern="/img/**" security="none"/>
    <http pattern="/js/**" security="none"/>
    <http pattern="/plugins/**" security="none"/>

    <!-- 配置拦截规则 -->
    <http use-expressions="false">
        <!-- 角色可访问路径 -->
        <intercept-url pattern="/**" access="ROLE_ADMIN"/>
        <!-- 登录配置 -->
        <form-login login-page="/login.html" login-processing-url="/login" default-target-url="/admin/index.html" always-use-default-target="true"/>
        <!-- 关闭跨域请求验证 -->
        <csrf disabled="true"/>
        <!-- 内置框架资源放行 -->
        <headers>
            <frame-options policy="SAMEORIGIN"/>
        </headers>
        <!-- 退出登录，默认访问/logout，默认跳转到登录页面 -->
        <logout/>
    </http>

    <!-- 认证管理器 -->
    <authentication-manager>
        <authentication-provider>
            <user-service>
                <user name="tom" password="123" authorities="ROLE_ADMIN"/>
                <user name="lucy" password="123" authorities="ROLE_ADMIN"/>
            </user-service>
        </authentication-provider>
    </authentication-manager>

</beans:beans>